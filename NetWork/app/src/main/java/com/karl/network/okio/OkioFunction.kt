package com.karl.network.okio

import okio.*
import java.io.File
import java.io.InputStream
import java.io.OutputStream
import java.net.Socket

/*fun File.source(): Source = Okio.source(this)
fun InputStream.source(): Source = Okio.source(this)
fun Socket.source(): Source = Okio.source(this)

fun Source.buffer(): BufferedSource = Okio.buffer(this)

fun File.sink(): Sink = Okio.sink(this)
fun OutputStream.sink(): Sink = Okio.sink(this)
fun Socket.sink(): Sink = Okio.sink(this)

fun Sink.buffer(): BufferedSink = Okio.buffer(this)*/


